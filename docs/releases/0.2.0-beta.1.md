# KlassID 0.2.0-beta.1 – Releasekandidat, Gate in Arbeit

PEP-440-Version: `0.2.0b1`. Benutzerbezeichnung: `0.2.0-beta.1`.

## Release-Schema

- Betas: `0.2.0-beta.N`, technisch `0.2.0bN`, Git-Tag `v0.2.0-beta.N`.
- Stabil: `0.2.0`, technisch und als Git-Tag `v0.2.0`.
- Ein Tag wird ausschließlich auf den gebauten, migrierten, ausgerollten und
  vollständig geprüften Releasecommit gesetzt.

## Aktueller Kandidatenumfang

Strukturell vorhanden und synthetisch geprüft sind Schule/Klasse/
Mitgliedschaften, geschützte Dokumente, Kalender, persönlicher Stundenplan,
Galerie, Chat, Events, Push-Grundtechnik, DB-Synchronisationspläne sowie
serverseitige Modulvererbung. Der Eltern-Beta-Kern ist implementiert:
kontrollierte Registrierung, E-Mail-Prüfung, administrative Freigabe,
Einmallink, persönliches Kontaktprofil, Kontaktverzeichnis, persönliche
Neuigkeitsstände und ein rate-limitierter Push-Selbsttest pro eigenem Gerät.
Deployment, öffentliche HTTPS-Smokes und Tag bleiben bis zum vollständig
grünen Abschlussgate offen.

## Modulstatus des Kandidaten

| Modul | Status | Nachweis/Grenze |
|---|---|---|
| Kalender | aktiv, synthetisch geprüft | bestehende Phase-8-Suite |
| WebUntis-Stundenplan | aktiv, zuvor real geprüft | bestehender JSON-RPC-Pilot |
| PDF-Formulare (geschützter Download) | aktiv, synthetisch geprüft | keine SmartForms-Funktion |
| Galerie | aktiv, synthetisch und responsiv geprüft | Browser-Gesamtgate bestanden |
| Chat | aktiv, synthetisch und responsiv geprüft | Browser-Gesamtgate bestanden |
| Events/Mitbringen | aktiv, synthetisch geprüft | Eltern-Browser-Smoke bestanden |
| Push-Grundtechnik | aktiv, pro Gerät synthetisch geprüft | realer Zusteller-Selbsttest folgt nach Deployment |
| WebUntis-Hausaufgaben | technisch real geprüft, nicht automatisch aktiviert | kontrollierter `/student-homework`-Abruf: 6 Hausaufgaben, 13 Unterrichtsbezüge, 6 Datensätze, 4 Lehrkraftreferenzen; keine Inhalte protokolliert |
| Photo Memory | deaktiviert | bleibt standardmäßig aus |
| itslearning | deaktiviert | nicht Teil des Minimalumfangs |
| Kontakte | aktivierbar | Klassenisolation, Freigaben und responsive Oberfläche geprüft |
| Mobilität | zurückgestellt | reines Planungsartefakt |
| PDF-SmartForms | zurückgestellt | reines Planungsartefakt |

## Migration und Rollback

Vor produktiver Migration: PostgreSQL- und Medienbackup, Image-ID und aktuellen
Releasecommit dokumentieren. Migrationen 0004-0006 erweitern Schule, Branding,
Hostname, Katalog, Module und Synchronisationsstatus ohne beabsichtigte
Datenlöschung. Rollback erfolgt durch Wiederherstellung des zusammengehörigen
Datenbank-/Medienbackups und des vorherigen Images; die Datenmigrationen werden
nicht blind rückwärts auf produktiven Daten ausgeführt.

## Datenschutz und Betrieb

`schools.csv`, private WebUntis-Referenzen, Zugangsdaten, Cookies,
Laufzeitantworten, Fotos und ausgefüllte Dokumente bleiben außerhalb von Git.
Der echte Schulbestand wurde nicht analysiert, weil die Datei im Worktree
fehlt. DNS, Resend, Caddy, Produktivmigration und Deployment auf `klassid.de`
wurden noch nicht vorgenommen.

## Anonymisiertes Beta-Gate vom 2. September 2026

- App und Web-Push-Kit: 147 Tests im abschließenden vollständigen Lauf
  bestanden.
- Vision: 33 Tests mit lokalen, nicht eingecheckten Modellgewichten bestanden.
- Der Push-Selbsttest adressiert ausschließlich die ausgewählte eigene
  Subscription. Fremdgerät, leerer Endpoint, Reaktivierung, stale/temporärer
  Fehler und Rate Limit sind geprüft.
- Ruff, compileall, Django `check`, Migrationsdrift, JavaScript-Syntax und
  `git diff --check` bestanden.
- `check --deploy` meldet nur die bekannten HSTS-Hinweise für
  `includeSubDomains` und `preload`; beide bleiben bis zur abgesicherten
  HTTPS-Abdeckung aller Subdomains zurückgestellt.
- Browser-E2E in 390×844, 820×1180 und 1440×900: Home, Kontakte,
  Schülerübersicht, Profil, Kalender/Hausaufgaben, WebUntis,
  Benachrichtigungen/App und Tutorial ohne horizontale Überbreite oder
  Serverfehler. Das Burger-Menü zeigt Eltern keine Administration.
- Registrierung, neutrale Empfangsantwort, E-Mail-Prüfung, Freigabe,
  einmalige Aktivierung und vollständiges Zehn-Schritte-Onboarding wurden im
  lokalen Browser geprüft. Zwei dabei gefundene Blocker für Eltern ohne
  bestätigtes Kind und für das gesperrte Biometriemodul wurden behoben.
- Der reale WebUntis-Abruf verwendete HomeOps-Secrets nur im kurzlebigen
  Prozess. Zugangsdaten, Cookies, Tokens, Rohantworten und Inhalte wurden weder
  ausgegeben noch gespeichert.

## Betrieb, Datenschutz und Rollback vor Deployment

- Vor produktiven Änderungen werden PostgreSQL, Medien, Container-/Image-IDs,
  Caddy-Konfiguration und DNS-Iststand gemeinsam gesichert. Backup und
  Releaseimage bilden die Rollbackeinheit.
- Rollback: bisheriges Image und bisherigen Caddy-Stand aktivieren, das
  zugehörige Datenbank-/Medienbackup restaurieren und danach interne sowie
  externe Health-/Login-Smokes ausführen. Produktive Migrationen werden nicht
  blind rückwärts angewendet.
- Push speichert nur technischen Endpoint, Schlüssel, Kontobezug und ein frei
  gewähltes datensparsames Gerätelabel. Der Selbsttest enthält keine Namen,
  Kinder- oder Fachdaten.
- Private Schuldatei, WebUntis-Referenzen, Zugangsdaten, Cookies,
  Laufzeitantworten, Fotos, Modellgewichte und Uploads bleiben außerhalb Git.
- Die Schuldatei war im aktuellen Worktree nicht vorhanden. Herkunft und
  Lizenz bleiben externer Blocker; es erfolgte kein Import. Der synthetische
  Importpfad prüft NFC, Umlaute und ß.
- Die öffentlichen A-Records für `klassid.de` und `5e.klassid.de` zeigen am
  Prüfzeitpunkt auf den Provider-Iststand. Proxy-/DNS-/Resend-Änderungen
  erfolgen erst nach exportiertem Iststand und konkret geprüftem Rückfallplan.

## Frühere Teilnachweise

- 14 Phase-2-Rechte-/Isolationstests bestanden.
- 18 Unicode-/Schulimport-/Hostname-/Rechteprüfungen bestanden.
- 32 betroffene Galerie-/Chat-/Kalender-/Biometrie-/Integrationsprüfungen
  bestanden; zwei Runner-Pfadfehler wurden im korrekten Layout erfolgreich
  wiederholt.
- 16 WebUntis-Regressionstests bestanden.
- 6 Scheduler-/DST-/Retry-/Push-Genau-einmal-Tests bestanden.
- Modul-/Login-/Push-Teilgate: 4 Prüfungen bestanden.

Dies ist noch kein finales Releaseprotokoll. Finaler Docker-Build,
Produktivbackup/-migration, HTTPS/TLS, Zusteller-Selbsttest,
Remote-Synchronisierung und Tag bleiben vor Freigabe Pflicht.

## Nächste getrennte Aufträge

1. [PDF-SmartForms-Integration](../planning/Naechste-Aufgabe-PDF-SmartForms-Integration.md)
2. [Mobilität und Mitfahrbörse](../planning/Naechste-Aufgabe-Mobilitaet-Mitfahrboerse.md)
