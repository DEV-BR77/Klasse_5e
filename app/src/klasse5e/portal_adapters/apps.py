from django.apps import AppConfig


class PortalAdaptersConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "klasse5e.portal_adapters"
    verbose_name = "Schulportal-Adapter"
