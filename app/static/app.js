(() => { const live=document.querySelector('#live-status'); const announce=t=>{if(live)live.textContent=t}; document.querySelectorAll('[data-confirm]').forEach(f=>f.addEventListener('submit',e=>{if(!window.confirm(f.dataset.confirm))e.preventDefault()})); document.querySelectorAll('[data-dialog-open]').forEach(b=>b.addEventListener('click',()=>document.getElementById(b.dataset.dialogOpen)?.showModal())); document.querySelectorAll('[data-dialog-close]').forEach(b=>b.addEventListener('click',()=>b.closest('dialog')?.close())); const chat=document.querySelector('[data-chat-poll]'); if(chat){const status=document.querySelector('[data-chat-status]');const url=chat.dataset.chatPoll;let latest=chat.dataset.latest||'';const poll=async()=>{try{const r=await fetch(`${url}${latest?`?since=${encodeURIComponent(latest)}`:''}`,{headers:{Accept:'application/json'}});if(!r.ok)throw new Error();const d=await r.json();if(d.messages?.length){latest=d.messages.at(-1).created_at;window.location.reload()}status.textContent=`Aktualisiert ${new Date().toLocaleTimeString('de-DE',{hour:'2-digit',minute:'2-digit'})}`}catch(_){status.textContent='Verbindung unterbrochen';announce('Chat-Verbindung unterbrochen. Erneut versuchen.')}};const timer=window.setInterval(poll,10000);window.addEventListener('beforeunload',()=>window.clearInterval(timer));document.querySelector('[data-chat-retry]')?.addEventListener('click',poll)}})();
